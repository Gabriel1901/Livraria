{% extends 'base.html.twig' %}

{% block body %}

    
         <h1>Lista de Produtos <a href="{{ path('produtos_new') }}" class="btn btn-primary pull-right">Novo produto</a></h1>
        
    {#<table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th>Quantidade</th>
                <th>Preço</th>
                <th>Tipo</th>
                <th>Imagem</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
        {% for produto in produtos %}
           <tr>
                <td><a href="{{ path('produtos_show', { 'id': produto.id }) }}">{{ produto.id }}</a></td>
                <td>{{ produto.nome }}</td>
                <td>{{ produto.quantidade }}</td>
                <td>{{ produto.preco }}</td>
                <td>{{ produto.tipo }}</td>
                <td>{{ produto.imagem }}</td>
                <td> 
                    <a href="{{ path('produtos_show', { 'id': produto.id }) }}" class="btn btn-default">
                        <span class="glyphicon glyphicon-eye-open"></span> </a>
                    <a href="{{ path('produtos_edit', { 'id': produto.id }) }}" class="btn btn-warning">
                        <span class="glyphicon glyphicon-pencil"></span>
                    </a>
                    
                </td>
            </tr>
    
    </tbody>
    </table>
#}
            
            
            <table id="cart" class="table table-hover table-condensed">
    				<thead>
						<tr>
                                                        
							<th style="width:35%">Item</th>
                                                        <th style="width:10%">Id</th>
							<th style="width:10%">Preço</th>
							<th style="width:8%">Quantidade</th>
							<th style="width:22%" class="text-center">Tipo</th>
							<th style="width:15%">Genero</th>
                                                        
						</tr>
					</thead>
					<tbody>
                                            {% for produto in produtos %}
						<tr>
							<td data-th="Product">
                                                            
								<div class="row">
									<div class="col-sm-2 hidden-xs"><img src="{{ produto.imagem }}" alt="..." class="img-responsive"/></div>
									<div class="col-sm-10">
										<h4 class="nomargin">{{ produto.nome }}</h4>
										<p></p>
									</div>
								</div>
							</td>
                                                        <td data-th="id"><a href="{{ path('produtos_show', { 'id': produto.id }) }}">{{ produto.id }}</a></td>
							<td data-th="Price">{{ produto.preco }}</td>
							<td data-th="Quantity">{{ produto.quantidade }}</td>
							<td data-th="Subtotal" class="text-center">{{ produto.tipo }}</td>
                                                        <td data-th="Subtotal" class="text-center">{{ produto.genero }}</td>
							<td> 
                                                            <a href="{{ path('produtos_show', { 'id': produto.id }) }}" class="btn btn-default">
                                                                <span class="glyphicon glyphicon-eye-open"></span> </a>
                                                            <a href="{{ path('produtos_edit', { 'id': produto.id }) }}" class="btn btn-warning">
                                                                <span class="glyphicon glyphicon-pencil"></span>
                                                            </a>

                                                        </td>
						</tr>
                                                {% endfor %}
					</tbody>
					
				</table>
            
            
        
        
  
    


{% endblock %}
